[pytest]
testpaths = tests
python_files = test_*.py
python_classes = *Tests
python_functions = test_*
markers =
    integration: end-to-end workspace and subprocess tests that are skipped unless --run-integration is passed
    slow: expensive tests such as LaTeX compilation that are skipped unless --run-slow is passed
